<%= form_for(@report_content) do |f| %>
  <% if @report_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_content.errors.count, "error") %> prohibited this report_content from being saved:</h2>

      <ul>
      <% @report_content.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
