<%= form_for(@report_date) do |f| %>
  <% if @report_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@report_date.errors.count, "error") %> prohibited this report_date from being saved:</h2>

      <ul>
      <% @report_date.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
